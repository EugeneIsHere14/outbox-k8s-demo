spring:
  application:
    name: order

  datasource:
    url: jdbc:mysql://mysql:3306/orders
    username: user
    password: pass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
