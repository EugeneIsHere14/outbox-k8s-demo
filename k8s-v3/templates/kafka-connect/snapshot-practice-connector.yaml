{{- if .Values.snapshotPracticeConnector.enabled }}
apiVersion: kafka.strimzi.io/v1
kind: KafkaConnector
metadata:
  name: customers-snapshot-connector
  namespace: kafka
  labels:
    strimzi.io/cluster: kafka-connect
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.snapshotPracticeConnector.syncWave }}"
spec:
  class: io.debezium.connector.mysql.MySqlConnector
  tasksMax: 1
  config:
    database.hostname: "{{ .Values.snapshotPracticeConnector.ordersDatabaseHost }}"
    database.port: "{{ .Values.snapshotPracticeConnector.ordersDatabasePort }}"
    database.user: "{{ .Values.snapshotPracticeConnector.ordersDatabaseUser }}"
    database.password: "{{ .Values.snapshotPracticeConnector.ordersDatabasePassword }}"
    database.server.id: "{{ .Values.snapshotPracticeConnector.serverId }}"
    topic.prefix: "{{ .Values.snapshotPracticeConnector.topicPrefix }}"
    database.include.list: "orders"
    table.include.list: "orders.customers_snapshot_test,orders.debezium_signals"
    schema.history.internal.kafka.bootstrap.servers: "kafka-kafka-bootstrap:9092"
    schema.history.internal.kafka.topic: "schemahistory.customers-snapshot"
    include.schema.changes: "false"
    snapshot.mode: "{{ .Values.snapshotPracticeConnector.snapshotMode }}"
    signal.enabled.channels: "source"
    signal.data.collection: "orders.debezium_signals"
    incremental.snapshot.chunk.size: "{{ .Values.snapshotPracticeConnector.incrementalSnapshotChunkSize }}"
    tombstones.on.delete: "false"
{{- end }}
